<Page x:Class="ErpSystemBeniSouef.Views.Pages.ReportsRegion.ReportsPages.Cash_SalesPage" 
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:userControls="clr-namespace:ErpSystemBeniSouef.Views.UserControls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ErpSystemBeniSouef.Views.Pages.ReportsRegion.ReportsPages"
      mc:Ignorable="d"  
      Title="Cash_SalesPage" 
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      FlowDirection="RightToLeft"
      Background="#1F2E3A" 
      d:DesignWidth="1700" d:DesignHeight="900"   >

    <Page.Resources>
        <SolidColorBrush x:Key="CardBrush" Color="#2B3A4A"/>
        <SolidColorBrush x:Key="PrimaryButtonBrush" Color="#0E6C8E"/>
        <SolidColorBrush x:Key="PrimaryButtonHoverBrush" Color="#1080A8"/>
        <SolidColorBrush x:Key="TextBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="MutetTextBrush" Color="#CCCCCC"/>
        <SolidColorBrush x:Key="GridHeaderBrush" Color="#304151"/>
        <SolidColorBrush x:Key="GridLineBrush" Color="#FFFFFF"/>

        <Style x:Key="MainButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="{StaticResource PrimaryButtonBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                             CornerRadius="8">
                            <ContentPresenter
                             HorizontalAlignment="Center"
                             VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryButtonHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SummaryTextBoxStyle" TargetType="TextBox">
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="#1E3040"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#304151"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,10,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                             BorderBrush="{TemplateBinding BorderBrush}"
                             BorderThickness="{TemplateBinding BorderThickness}"
                             CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="2"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridCell" x:Key="RightAlignedDataGridCell">
            <Setter Property="TextBlock.TextAlignment" Value="Right"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                    <Setter Property="Background" Value="{StaticResource PrimaryButtonBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGrid" x:Key="CashDataGridStyle">
            <Setter Property="Background" Value="#1E3040"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="RowBackground" Value="#253748"/>
            <Setter Property="AlternatingRowBackground" Value="#2B3A4A"/>

            <Setter Property="GridLinesVisibility" Value="All"/>
            <Setter Property="VerticalGridLinesBrush" Value="{StaticResource GridLineBrush}"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource GridLineBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>

            <Setter Property="RowHeight" Value="32"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="ColumnHeaderHeight" Value="36"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="FlowDirection" Value="RightToLeft"/>

            <Setter Property="CellStyle" Value="{StaticResource RightAlignedDataGridCell}"/>

            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{StaticResource GridHeaderBrush}"/>
                        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="8,0"/>
                        <Setter Property="HorizontalContentAlignment" Value="Right"/>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="42" />
            <RowDefinition Height="*" /> 
            <RowDefinition Height="82"/>
        </Grid.RowDefinitions>
        <userControls:TitleBar FlowDirection="LeftToRight"/>


        <Grid Margin="30 30 30 2" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="430"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" 
             Background="{StaticResource CardBrush}" 
             CornerRadius="10" 
             Padding="10" 
             Margin="0,0,15,0">

                <DataGrid x:Name="CashReportsDataGrid" FlowDirection="LeftToRight"
                          HorizontalAlignment="Center"  FontSize="24" Style="{StaticResource CashDataGridStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn FontSize = "18"   Header="المبلغ الإجمالي" Binding="{Binding TotalPrice}" Width=".9*"/>
                        <DataGridTextColumn FontSize = "18"   Header="المبلغ" Binding="{Binding UnitPrice}" Width=".5*"/>
                        <DataGridTextColumn FontSize = "18"   Header="الكمية" Binding="{Binding Quentity}" Width=".5*"/>
                        <DataGridTextColumn FontSize = "18"   Header="المبيعات" Binding="{Binding ProductName}" Width="*"/>
                        <DataGridTextColumn FontSize = "18"   Header="نوع السلعة" Binding="{Binding ProductType}" Width="1.2*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Border Grid.Column="1" 
     Background="{StaticResource CardBrush}" 
     CornerRadius="10" 
     Padding="20" Margin="10,0,0,0">

                <StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
                        <TextBlock Text="المقدمات" 
                    Foreground="{StaticResource TextBrush}" 
                    VerticalAlignment="Center" 
                    Margin="0,0,10,0" 
                    Width="165"
                    FontSize="16"/>

                        <Grid Width="200" HorizontalAlignment="Right">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="70"/>
                            </Grid.ColumnDefinitions>

                            <TextBox Grid.Column="0" 
                      Style="{StaticResource SummaryTextBoxStyle}" 
                      Width="Auto" Text="0"
                      Margin="0,0,5,0" 
                       />
                            <Button Content="طباعة" 
                     Grid.Column="1" 
                     Style="{StaticResource MainButtonStyle}" 
                     Height="36" 
                     FontSize="12"
                     Margin="5,0,0,0"/>
                        </Grid>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
                        <TextBlock Text="إجمالي الكاش" 
                    Foreground="{StaticResource TextBrush}" 
                    VerticalAlignment="Center" 
                    Margin="0,0,10,0"  
                    Width="165"
                    FontSize="16"/>

                        <Grid Width="200" HorizontalAlignment="Right">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="70"/>
                            </Grid.ColumnDefinitions>

                            <TextBox Grid.Column="0" 
                      Style="{StaticResource SummaryTextBoxStyle}" 
                      Width="Auto" Name="TotalCashReport"
                      Margin="0,0,5,0" 
                      Text=""/>
                            <Button Content="طباعة" 
                     Grid.Column="1" 
                     Style="{StaticResource MainButtonStyle}" 
                     Height="36" 
                     FontSize="12"
                     Margin="5,0,0,0"/>
                        </Grid>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
                        <TextBlock Text="نسبة المندبة الأولية" 
                    Foreground="{StaticResource TextBrush}" 
                    VerticalAlignment="Center" 
                    Margin="0,0,10,0" 
                    Width="165"
                    FontSize="16"/>

                        <Grid Width="200" HorizontalAlignment="Right">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="70"/>
                            </Grid.ColumnDefinitions>

                            <TextBox Grid.Column="0" 
                      Style="{StaticResource SummaryTextBoxStyle}" 
                      Width="Auto" 
                      Margin="0,0,5,0" 
                      Text=""/>
                            <Button Content="طباعة" 
                     Grid.Column="1" 
                     Style="{StaticResource MainButtonStyle}" 
                     Height="36" 
                     FontSize="12"
                     Margin="5,0,0,0"/>
                        </Grid>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
                        <TextBlock Text="نسبة المرتجعات" 
                    Foreground="{StaticResource TextBrush}" 
                    VerticalAlignment="Center" 
                    Margin="0,0,10,0" 
                    Width="165"
                    FontSize="16"/>
                        <TextBox Style="{StaticResource SummaryTextBoxStyle}" 
                  Width="200" 
                  Text=""
                  Margin="0,0,0,0"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="صافي النسبة" 
                    Foreground="{StaticResource TextBrush}" 
                    VerticalAlignment="Center" 
                    Margin="0,0,10,0" 
                    Width="165"
                    FontSize="16"/>
                        <TextBox Style="{StaticResource SummaryTextBoxStyle}" 
                  Width="200" 
                  FontWeight="ExtraBold" 
                  Background="#304151" 
                  IsEnabled="False"
                  Text=""
                  Margin="0,0,0,0"/>
                    </StackPanel>


                    <StackPanel  HorizontalAlignment="Center" Margin="5,220,0,0">

                        <ProgressBar
        x:Name="LoadingBar"
        Width="270"
        Height="6"
        IsIndeterminate="True"
        Visibility="Collapsed"/>

                        <TextBlock
        x:Name="LoadingText"
        Foreground="White"
        FontSize="15"
        Margin="0,10,0,0"
        TextAlignment="Center"
        Visibility="Collapsed"/>
                    </StackPanel>


                    <Button Content="صفحه المبيعات كاش" 
Grid.Column="1"  Background="Red" 
Style="{StaticResource MainButtonStyle}" 
Height="40" Width="270"
FontSize="17"
Margin="5,20,0,0" />

                </StackPanel>
            </Border>
        </Grid>

        <!-- زر الرجوع -->
        <Button Grid.Row="2"
VerticalAlignment="Bottom"
HorizontalAlignment="Right"
Content="↩"
FontSize="35"
Width="90"
Height="58"
Foreground="White"
Style="{StaticResource triggerrr}"
Margin="21,10,60,11"
Click="BackBtn_Click"/>

    </Grid>
</Page>
