<Page x:Class="ErpSystemBeniSouef.Views.Pages.CollectionRegion.SelectCollectionTabPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ErpSystemBeniSouef.Views.Pages.CollectionRegion"
      Background="#1F2E3A"
      FlowDirection="RightToLeft"
      xmlns:userControls="clr-namespace:ErpSystemBeniSouef.Views.UserControls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SelectCollectionTabPage">


    <Page.Resources>
        <SolidColorBrush x:Key="CardBrush" Color="#2B3A4A"/>
        <SolidColorBrush x:Key="PrimaryButtonBrush" Color="#0E6C8E"/>

        <Style x:Key="LargeMenuButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="70"/>
            <Setter Property="Width" Value="350"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource PrimaryButtonBrush}"/>
            <Setter Property="Margin" Value="0,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                              CornerRadius="12" 
                              BorderBrush="#304151" 
                              BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0A5A78"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="66"/>
            <!-- Title bar -->
            <RowDefinition Height="*"/>
            <!-- Page title -->

            <RowDefinition Height=".2*"/>

        </Grid.RowDefinitions>
        <userControls:TitleBar FlowDirection="LeftToRight" Grid.Row="0" Margin="0,0,0,8"/>


        <Grid Grid.Row="1">
            <Border Background="{StaticResource CardBrush}" 
              CornerRadius="15" 
              Padding="30" 
              HorizontalAlignment="Center" 
              VerticalAlignment="Center">

                <StackPanel>
                    <TextBlock Text=" التحصيل " 
                         Foreground="White" 
                         FontSize="24" 
                         FontWeight="Bold" 
                         HorizontalAlignment="Center" 
                         Margin="0,0,0,30"/>

                    <Button   Cursor="Hand" Content="المبالغ المحصلة" Style="{StaticResource LargeMenuButtonStyle}" Click="Button_Click"/>
                    <Button   Cursor="Hand" Content="الزوايد" Style="{StaticResource LargeMenuButtonStyle}"/>
                    <Button   Cursor="Hand" Content="النواقص" Style="{StaticResource LargeMenuButtonStyle}"/>
                </StackPanel>

            </Border>
        </Grid>

        <Grid  Grid.Row="2" Margin=" 0 0 50 30" FlowDirection="LeftToRight">
            <Button Grid.Row="4" VerticalAlignment="Bottom" HorizontalAlignment="Left" 
            Content="↩" FontSize="35" 
   Width="90" Height="48" Foreground="White"
   Style="{StaticResource triggerrr}" Margin="1,0,0,1"  
   Click="BackBtn_Click"/>
        </Grid>
    </Grid>
</Page>
