<Page x:Class="ErpSystemBeniSouef.Views.Pages.CovenantRegion.CovenantItemsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ErpSystemBeniSouef.Views.Pages.CovenantRegion"
      xmlns:userControls="clr-namespace:ErpSystemBeniSouef.Views.UserControls"
      mc:Ignorable="d"  
      Title="CovenantItemsPage"
      
      FlowDirection="LeftToRight"
      Background="#1F2E3A"
      d:DesignWidth="1920" d:DesignHeight="1080">


    <Page.Resources>
        <SolidColorBrush x:Key="CardBrush" Color="#2B3A4A"/>
        <SolidColorBrush x:Key="PrimaryButtonBrush" Color="#0E6C8E"/>

        <Style x:Key="BlackTextComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Width" Value="135"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="#F0F0F0"/>
        </Style>

        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="Background" Value="#1E3040"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#304151"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource PrimaryButtonBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="78"/>
        </Grid.RowDefinitions>
        <userControls:TitleBar FlowDirection="LeftToRight"/>

        <Grid Margin="10" Grid.Row="1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource CardBrush}" CornerRadius="10" Padding="5" Margin="0,0,10,0">
            <DataGrid Background="#1E3040" Foreground="White" AutoGenerateColumns="False" GridLinesVisibility="All" VerticalGridLinesBrush="White" HorizontalGridLinesBrush="White">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#304151"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="BorderBrush" Value="White"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="الرقم المسلسل" Binding="{Binding Serial}" Width="*"/>
                    <DataGridTextColumn Header="اسم المنتج" Binding="{Binding ProductName}" Width="*"/>
                    <DataGridTextColumn Header="الكمية" Binding="{Binding Quantity}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Column="1" FlowDirection="RightToLeft" Background="{StaticResource CardBrush}" CornerRadius="10" Padding="15">
            <StackPanel>
                <TextBlock Text="نوع المنتج" Foreground="White" Margin="0,0,0,5"/>
                <ComboBox Name="productTypeComo" Style="{StaticResource BlackTextComboBox}" SelectionChanged="SelectProductType"/>

                <TextBlock Text="اسم المنتج" Foreground="White" Margin="0,0,0,5"/>
                    <ComboBox  Name="ProductCombo"  Style="{StaticResource BlackTextComboBox}"     
                               HorizontalAlignment="Center"
                               DisplayMemberPath="ProductName"  
                               SelectedValuePath="Id"/>

                <TextBlock Text="عدد القطع" Foreground="White" Margin="0,0,0,5"/>
                <TextBox   Name="Quantity" Style="{StaticResource ModernTextBoxStyle}"/>

                <UniformGrid Columns="2" Margin="0,10,0,0">
                    <Button Content=" اضافة عنصر " Style="{StaticResource ActionButtonStyle}" Margin="0,0,5,0" Click="AddConvnenantItem"/>
                    <Button Content="حذف" Style="{StaticResource ActionButtonStyle}" Background="#A93226" Margin="5,0,0,0"/>
                </UniformGrid>
                    <Button Content=" اضافة المعلومات كلها " Style="{StaticResource ActionButtonStyle}" Margin="0,220,5,0" Click="AddConvnenant"/>

                </StackPanel>
        </Border>
    </Grid>

    <Grid Grid.Row="2">
        <StackPanel  HorizontalAlignment="Center" Margin="5,20,0,0">

            <ProgressBar
x:Name="LoadingBar"
Width="270"
Height="6"
IsIndeterminate="True"
Visibility="Collapsed"/>

            <TextBlock
x:Name="LoadingText"
Foreground="White" 
FontSize="15"
Margin="0,10,0,0"
TextAlignment="Center"
Visibility="Collapsed"/>
        </StackPanel>

        <!-- زر الرجوع -->
        <Button Grid.Row="1"
VerticalAlignment="Bottom"
HorizontalAlignment="Left"
Content="↩"
FontSize="35"
Width="90"
Height="54"
Foreground="White"
Style="{StaticResource triggerrr}" 
Margin="21,10,50,14" Click="BackBtn_Click"  />
    </Grid>

    </Grid >
</Page>
